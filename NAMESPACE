#exportPattern("^[[:alpha:]]+")
export(arrangeC, ceplot, cont2color, factor2color, mydist, plotxc, plotxs, 
    savingby2d, visualweight, update.xcplot, update.xsplot)
import(graphics)
importFrom(MASS, parcoord)
