export(arrangeC, ceplot, cont2color, factor2color, interpolate,
  interpolate.numeric, interpolate.integer, interpolate.factor,
  interpolate.character, dist1, plotxc, plotxs, savingby2d, similarityweight,
  condtour, makepath)
import(graphics, grDevices, stats)
importFrom(utils, head, tail)
importFrom(MASS, parcoord)
